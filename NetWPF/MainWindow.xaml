<Window x:Class="NetWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBox Name="Username" Width="Auto" MinWidth="50" Margin="0 0 20 0"></TextBox>
            <TextBox Name="Password" Width="Auto" MinWidth="50"></TextBox>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <Button Click="Connect_OnClick">Connect</Button>
            <Button Click="GetQuote_OnClick">Get quote</Button>
            <TextBlock Name="Quote"></TextBlock>
        </StackPanel>
    </Grid>
</Window>
